# 2026-07-27 工程审计整改对账

> 本文对 `eng-audit-comprehensive-2026-07-27.md` 的 39 项结论逐项核实和对账。状态以源码、提交和测试证据为准；“阶段性治理”不等于原问题已经关闭。

- 日期：2026-07-27
- 分支：`work/eng-audit-fixes-20260727`
- 整改基线：`65703faa4557901814b5bbb059618c2de8424357`
- 代码整改截止：`56ecd9e937cbada2bd0e8e9d4973bfcba4d7d918`
- 代码整改提交数：48
- 提交口径：原 39 项对应 40 个整改提交，额外复核对应 8 个整改提交；不包含对账文档提交。

## 结论

- 已完成：25 项。
- 阶段性治理：9 项。
- 误报或不适用：2 项。
- 长期治理：1 项。
- 正面项：2 项。
- 额外复核发现并完成：8 项。

“已完成”表示审计条目描述的具体缺口已由当前实现和测试闭合。“阶段性治理”表示已经降低风险或拆出第一阶段，但原条目的广泛问题仍然存在。“长期治理”表示当前未发现可触发故障，贸然局部修改反而会扩大风险，需要随架构演进处理。

## 逐项对账

| #   | 状态         | 主要提交                 | 当前结论与剩余风险                                                                                                                                 |
| --- | ------------ | ------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | 阶段性治理   | `2dbce708a`              | WAL 读请求已使用独立只读连接，释放读写串行；共享写连接、全局写锁、`synchronous=FULL` 和语句看门狗仍未治理。                                        |
| 2   | 已完成       | `bff4cef05`              | 已补 systemd、launchd、NSSM、Docker、升级回滚、备份恢复和事故处置文档；后续仍需随生产流程校准。                                                    |
| 3   | 已完成       | `a9761d2f3`              | 已增加正式架构说明和文档索引，并把历史审计、基线和 RFC 迁入 `docs/audit/`、`docs/rfc/`。                                                           |
| 4   | 已完成       | `d072bd334`              | Gateway、Health Controller 已迁入 `web/`，`bootstrap/` 不再承载控制器；尚无禁止回流的静态门禁。                                                    |
| 5   | 已完成       | `b3e2673ed`              | 工具层改为依赖端口，已无 `tool -> web` import；`web -> tool` 的单向依赖仍按现有架构保留。                                                          |
| 6   | 阶段性治理   | `4a04f31c2`              | 已抽出模型工厂和 dialect 注册边界，但 `SolonAiLlmGateway` 仍约 5,123 行，响应解析、用量、错误和编排职责仍集中。                                    |
| 7   | 阶段性治理   | `ec080abfd`              | 修复 Web 测试自动发现和一批陈旧断言；当前 128 个测试仍以静态源码契约为主，核心交互行为覆盖仍不足。                                                 |
| 8   | 已完成       | `86929408f`              | 来源会话过滤已下推为 SQLite 索引范围查询，Java 只对收窄后的候选做语义复核。                                                                        |
| 9   | 已完成       | `d6ab2ceb6`、`1a072f437` | Agent Run 批量、搜索、用量和陈旧运行查询均改为专用投影，已无 `select * from agent_runs`。                                                          |
| 10  | 已完成       | `4b8e95afc`              | 工具调用计数改为按终态转换原子增减，覆盖重复终态、回退和跨 Run 迁移。                                                                              |
| 11  | 已完成       | `296c3093e`              | 陈旧运行恢复改为单事务批量选择、批量更新和批量恢复记录，失败会整体回滚。                                                                           |
| 12  | 已完成       | `d8702c1cc`              | README 已与当前 Java 顶层包树对齐，并明确子包清单不是穷举。                                                                                        |
| 13  | 已完成       | `228c7f910`              | 已补认证、HMAC、Dashboard API、TUI WebSocket 和错误响应文档；手写文档仍需人工防漂移。                                                              |
| 14  | 已完成       | `caebbc93c`、`f2068e2da` | README 明确对外品牌为 Solon Claw，命令、制品、镜像和配置使用 `solonclaw`；Java 包仅为实现命名空间，文档索引标题也已对齐。                          |
| 15  | 已完成       | `d09ea8ae0`              | 已记录异常分层、客户端脱敏、LLM 分类重试、取消中断和排查流程。                                                                                     |
| 16  | 阶段性治理   | `8d3e9cf5d`              | Engine 已不直接依赖 SQLite 存储实现并增加静态边界测试；仍有约 9 个到 gateway、llm、tool 具体实现的依赖。                                           |
| 17  | 已完成       | `b66319c7b`              | Profile 子容器工厂改为 gateway 端口，具体装配移入 bootstrap，gateway 已无 bootstrap import。                                                       |
| 18  | 阶段性治理   | `0676ecaea`              | Profile 网关生命周期已下沉，`ProfileManager` 从约 3,943 行降至约 3,513 行；其他 2,000 至 5,000 行大类仍需继续拆分。                                |
| 19  | 误报或不适用 | 无                       | `VideoGenProvider` 只有无实现、无调用方的接口和值对象，报告所述请求构造、超时和错误映射并不存在；未来新增实现时再补协议测试。                      |
| 20  | 已完成       | `d613415f1`、`bfd93dd5f` | 用量回填已覆盖失败重跑、提交后失败、10,001 条分页和重复执行幂等；大历史量性能尚未压测。                                                            |
| 21  | 长期治理     | 无                       | 当前未发现事务内再次获取共享写连接的真实路径，普通同线程 LIFO 嵌套也不会提前释放锁；共享连接代理仍较脆弱，应随单写者架构收口并补嵌套生命周期测试。 |
| 22  | 已完成       | `e209b767e`              | `sourceLocks` 改为引用计数租约，最后一个持有者或等待者关闭后删除键，覆盖并发、嵌套、幂等关闭和 1,000 键回收。                                      |
| 23  | 已完成       | `94767ecf3`              | 报告列举的数据路径已补低敏日志、显式降级和损坏状态拒绝覆盖；其他容错分支仍需区分数据完整性与安全 fail-closed。                                     |
| 24  | 阶段性治理   | `49a936ba5`              | 两个文本 Action 解析点已从 `Throwable` 收窄为 `RuntimeException`；生产代码仍有约 20 个 `catch (Throwable)`，不能宣称全仓完成。                     |
| 25  | 阶段性治理   | `ec37f96bc`              | 一批结构化错误和护栏测试已改用状态字段；当前仍约 107 个测试文件、728 行匹配中文文案。                                                              |
| 26  | 已完成       | `d50fb2dba`              | 已补 CDP 超时、WebSearch 请求与 503、STT 429、非法 JSON、超时和 multipart 契约测试；真实公网 Provider 仍依赖集成环境。                             |
| 27  | 已完成       | `5650821cb`、`b0c2fb07e` | 长期 Bearer 只用于换取 HttpOnly 短会话，旧 localStorage 令牌会清除，并补 SameSite、Secure、同源校验和 CSP；多实例仍需共享会话。                    |
| 28  | 已完成       | `629d4c1fe`              | 两个会话仓储统一复用 `SessionRecordMapper` 的列清单和映射，并用全字段一致性测试约束。                                                              |
| 29  | 阶段性治理   | `060f27e2b`              | Agent Run 的事件、控制、消息、工具、子代理、恢复和清理已拆为叶子组件；主门面及其他大型 SQLite 仓储仍需继续拆分。                                   |
| 30  | 已完成       | `1924828d0`、`4b52b9c77` | HTTP 5xx 已统一返回固定公共文案；复核发现并修复 Chat SSE `run.failed` 和 `tool.completed` 的异常消息、类名、路径和 SQL 文本泄露。                  |
| 31  | 已完成       | `652f84079`              | README 与 `config.example.yml` 的超时、安全网站黑名单和审批模型字段已对齐。                                                                        |
| 32  | 已完成       | `ca723284e`              | 飞书、QQBot、企微、微信和元宝均已补准备、最小配置、验证与排查文档；真实租户联调仍受外部环境限制。                                                  |
| 33  | 已完成       | `7f52e7b88`              | Bearer 和网关注入统一使用常量时间比较。                                                                                                            |
| 34  | 已完成       | `6ae00d39c`              | `DashboardRuntimeConfigService` 从约 923 行降至约 294 行，Catalog、Store 与公开门面分离；Catalog 仍较大但主要承载稳定声明数据。                    |
| 35  | 已完成       | `75e3f6ce1`              | 敏感配置 set、reveal 已追加式审计操作、认证方式、Profile、键名、IP、时间和事件 ID；审计不可用时 fail-closed。                                      |
| 36  | 正面项       | 无                       | 中文 Javadoc 和配置注释抽检仍成立，新增 Profile 生命周期及 Dashboard 配置类也保持中文说明。                                                        |
| 37  | 正面项       | `3a0eb29a8`              | Web 技术栈、构建和设计文档健康，源码目录说明已与当前结构对齐；尚无视觉文档自动回归。                                                               |
| 38  | 误报或不适用 | 无                       | Git 不跟踪空目录，HEAD 中主源码和测试均无 `mcp/` 文件；本机删除后的空目录不会进入克隆或发布物。                                                    |
| 39  | 阶段性治理   | `aad2c6655`              | 已增加无密钥的本地 Responses SSE 回放，覆盖网关、流式解析、持久化和投递；工具调用、重试、中断和其他 dialect 仍受 Live 门控。                       |

## 额外审查修复

- `ef8ea8402`：TUI WebSocket 改为短时、一次性票据，避免在连接 URL 中携带长期 Dashboard Bearer；多实例票据共享仍未实现。
- `a59045d63`：补齐 TUI 运行时控制器的构造装配，防止安全异常测试绕过真实 Bean 路径。
- `7b5c04522`：架构文档审查发现命名 Profile 独立网关进程会继承父进程完整环境；现已使用 Profile 自身 `.env` 和运行时环境快照，阻止父环境泄露与伪造 Profile 路径。`default` Profile 保留原继承行为，避免兼容性回归。
- `7917ea105`：移除 loopback 匿名握手例外，关闭反向代理同机部署下可被外部请求利用的认证绕过。
- `960515af1`：远程非 HTTPS 地址在发送显式 Dashboard 凭据前立即失败；loopback 和 HTTPS 保持可用。
- `dc2118696`：TUI 握手失败日志只记录脱敏后的 URL，避免一次性票据和其他查询参数进入日志。
- `43121fda1`：强制 Monaco Editor 使用修复后的 DOMPurify，确保生产 bundle 不再捆绑已知漏洞版本。
- `56ecd9e93`：握手返回、直连网关和事件 sidecar 的远程 WebSocket 强制使用 `wss:`，仅明确 loopback 允许 `ws:`。

## 聚焦验证

- SQLite 并发测试：3 项通过。
- Profile 生命周期实现验证：78/78 通过；独立复审再次运行 61 项并通过 Spotless。
- Dashboard 运行时配置拆分：107 项相关测试通过，60 项配置定义与拆分前逐项一致。
- #30 异常边界：修复前测试稳定复现路径、SQL 和类名泄露；修复后 18 项聚焦测试通过，独立复核 22 项通过。
- 架构文档：当前 Markdown 链接检查无缺失，Prettier 检查通过。
- TUI 认证链路：一次性票据、loopback 强制认证、远程 HTTP 与 WebSocket 明文拒绝、握手日志脱敏均有回归测试覆盖。
- DOMPurify 正反对照：当前隔离构建的生产 bundle 只出现 `3.4.12`，`3.2.7` 为 0；父提交隔离构建只出现 `3.2.7`，`3.4.12` 为 0，证明修复已进入实际生产 bundle。

## 最终门禁

- 后端干净完整构建：`mvn clean package`，共运行 2,764 项测试，0 失败、0 错误、3 跳过；成功生成 shaded JAR 与工作区发布目录。
- Java 格式：`mvn spotless:check`，946 个 Java 文件均满足格式要求。
- Web：128/128 个测试文件通过；生产构建成功，`npm audit --omit=dev` 为 0 个漏洞。
- Terminal UI：106 个测试文件通过，1,154 项通过、1 项跳过；`npm run type-check`、`npm run lint` 均成功，`npm audit --omit=dev` 为 0 个漏洞。
- 部署配置：`docker compose config` 成功，容器继续启用只读根文件系统、全能力移除和 `no-new-privileges`。
- 仓库门禁：项目命名、原始异常日志和 `git diff --check` 检查均通过。
- 文档门禁：本对账文档通过 Prettier；本轮未修改链接，既有 12 个 Markdown 链接检查结果保持无缺失。

## 源报告勘误

源报告作为审计时点记录保留；后续源码复核形成以下勘误，最终判断以本对账为准：

1. 39 项结论的实际严重度分布为严重 7 项、高 13 项、中 12 项、低 7 项；源报告写作 `7 / 13 / 9 / 5`，合计仅 34 项。
2. 源报告“未发现鉴权绕过”的判断已被后续复核推翻：`7917ea105` 修复了 loopback 匿名握手绕过；同轮还发现并修复远程 HTTP 与 WebSocket 明文传输、握手 URL 日志泄露和生产 bundle 中的 DOMPurify 漏洞版本。

## 未关闭风险

后续治理重点不是继续扩大本轮提交，而是按独立计划处理 9 项阶段性治理：

1. #1：SQLite 共享写连接、全局锁、`synchronous=FULL` 和慢语句观测。
2. #6：继续拆分约 5,123 行的 `SolonAiLlmGateway`。
3. #7：提高 Web 真实组件交互、错误态和行为测试比例。
4. #16：收口 Engine 到 gateway、llm、tool 具体实现的剩余依赖。
5. #18：继续拆分 `ProfileManager`、`AgentRunSupervisor`、审批服务和渠道适配器等超大类。
6. #24：逐个核实并收窄生产代码中剩余约 20 个 `catch (Throwable)`。
7. #25：以稳定业务错误码和结构字段替代残余中文文案断言。
8. #29：继续拆分大型 SQLite 仓储门面和其他聚合仓储。
9. #39：补齐工具调用、重试、中断和多 dialect 的确定性网关回放。

#21 仍属于长期治理：当前未发现共享连接代理的嵌套生命周期故障路径，但该设计较脆弱，应随 SQLite 单写者架构演进并补嵌套与乱序关闭测试。
