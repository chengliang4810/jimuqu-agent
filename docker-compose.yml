services:
  solon-claw:
    image: ghcr.io/chengliang4810/solon-claw:latest
    container_name: solon-claw
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - ./runtime:/app/runtime
