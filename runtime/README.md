# runtime

Runtime state is intentionally file-backed and workspace-visible at execution time.

This directory is kept in git only as a placeholder. Generated runs, sessions, routes,
dedup state, jobs, processes, and audit files stay untracked.
