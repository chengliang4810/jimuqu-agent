# Snack4 Recipes For jimuqu-agent

- Use `ONode.loadStr(...)` or typed deserialize helpers for dynamic payloads.
- Use `TypeRef` when deserializing generic collections or maps.
- Use Snack4-backed Solon serialization for controller DTOs.
- Preserve chat session snapshots as NDJSON using Solon AI chat message helpers rather than ad hoc JSON arrays.
